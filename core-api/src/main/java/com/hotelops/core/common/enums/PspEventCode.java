package com.hotelops.core.common.enums;

/** ENM-007 — PSP event vocabulary echoed by payments-sim. */
public enum PspEventCode {
    AUTHORISATION,
    CAPTURE,
    CAPTURE_FAILED,
    CANCELLATION,
    REFUND,
    REFUND_FAILED,
    AUTH_EXPIRY
}
