# Keep the build context lean and reproducible: never ship local build output or
# IDE/VCS cruft into the image. The Gradle build inside the Dockerfile is hermetic.
build/
.gradle/
.idea/
*.iml
.git/
HELP.md
